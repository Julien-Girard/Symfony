{% extends 'base.html.twig' %}

{% block title %}Titre{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
	<h1>Creation d'un nouveau theme :</h1>
    {{ form_start(formulaire) }}
    {{ form_label(formulaire.sujet) }}
    {{ form_widget(formulaire.sujet) }}
    </p>
    {{ form_label(formulaire.createur) }}
    {{ form_widget(formulaire.createur) }}
    </p>
    {{ form_label(formulaire.valider) }}
    {{ form_widget(formulaire.valider) }}
    {{ form_end(formulaire) }}
    </div>
{% endblock %}


