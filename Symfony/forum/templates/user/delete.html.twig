{% extends 'base.html.twig' %}

{% block title %}Titre{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
	<h1>Creation d'un nouvel utilisateur :</h1>
    {{ form_start(formulaire) }}
    {{ form_label(formulaire.pseudo) }}
    {{ form_widget(formulaire.pseudo) }}
    <p>
    {{ form_label(formulaire.passwd) }}
    {{ form_widget(formulaire.passwd) }}
    </p>
    {{ form_label(formulaire.about) }}
    {{ form_widget(formulaire.about) }}
    </p>
    {{ form_label(formulaire.valider) }}
    {{ form_widget(formulaire.valider) }}
    {{ form_end(formulaire) }}
    </div>
{% endblock %}


